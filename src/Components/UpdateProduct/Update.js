import React from "react";
import { useParams } from "react-router-dom";

const Update = () => {
  const {  _id } = useParams();
  return (
    <div>
      <h3></h3>
    </div>
  );
};

export default Update;
